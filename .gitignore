# python
__pycache__/
*.pyc

# env / local config
.env
.env.*

# Local-only working material: agent skills + their lockfile, design/
# compatibility notes, task plans and the working roadmap. Kept on disk for
# development, never published.
.agents/
docs/
tasks/
plan.md
skills-lock.json

# The Omarchy plugin's rendered state lives outside the tree (the installer
# writes ~/.local/state/najm-downloads/installed.json, never into this repo).
